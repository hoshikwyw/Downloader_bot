aiogram>=3.13,<4
yt-dlp>=2025.1.15
python-dotenv>=1.0
# yt-dlp plugin: fetches YouTube proof-of-origin tokens from the local provider
# (the Node server is built into the Docker image and runs on 127.0.0.1:4416).
bgutil-ytdlp-pot-provider
